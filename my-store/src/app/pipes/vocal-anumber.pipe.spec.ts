import { VocalANumberPipe } from './vocal-anumber.pipe';

describe('VocalANumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalANumberPipe();
    expect(pipe).toBeTruthy();
  });
});
